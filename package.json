{
  "name": "jpbank-coin-handling-fee-simulator",
  "version": "3.0.0",
  "description": "所持している硬貨をゆうちょATMに預け入れる時に、差し引かれる硬貨預払料金の割合を最小化するためのツール",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc",
    "rm": "rm -rf build",
    "build": "npm run rm && cp -r src build && rm -rf build/ts && npm run compile",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "typescript": "^5.7.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/matchaism/jpbank-coin-handling-fee-simulator.git"
  },
  "homepage": "https://jp-bank-coin-handling-fee-sim.matchaism.net"
}
